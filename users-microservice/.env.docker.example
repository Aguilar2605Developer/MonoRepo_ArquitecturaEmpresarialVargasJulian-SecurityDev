# Copia este archivo como ".env.docker" dentro de users-microservice/ y
# rellena los valores reales. ".env.docker" NUNCA se sube a git.
#
# DB_URL / DB_USERNAME / DB_PASSWORD los sobrescribe docker-compose.yml
# apuntando al Postgres containerizado local (postgres-users).

COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
SERVER_PORT=8686
